{% extends 'empty.html.twig' %}

{% block title %}Hotels{% endblock %}
 
{% block body %}

     <td>
                                <a href="{{ path('reservationhotel_list') }}">
                                    <button type="button" class="btn btn-info">Réserver un Hotel!</button>                            </p>

                            </a>
                                    </td>
    <table class="table">
        
        <tbody>
         <table class="table">
                                <thead>
                                <tr>
                                    <th>Date Début</th>
                                    <th>Date Fin</th>
                                    
                                    <th>Nombre Personnes</th>
                                    <th>Prix</th>
                                    <th>Modifier</th>
                                    <th>Annuler</th>
                                    
                                   
                                </tr>
                                </thead>
                                <body>
                                {% for p in  reservationhotels %}
                                    <tr>
                                        <td>{{ p.DateDebut|date('d-m-Y') }}</td>
                                        <td>{{ p.DateFin|date('d-m-Y') }}</td>
                                         
                                        <td>{{ p.nbrperso }}</td>
                                        <td>{{ p.prixtotal * p.nbrperso }}</td>
                                     <td>  <a href="/editreservationhotel/{{ p.id }}">
                                    <button type="button" class="btn btn-warning">Modifier</button>                            </p>

                            </a></td>
                                      <td>  <a href="/deletereservationhotel/{{ p.id }}">
                                    <button type="button" class="btn btn-danger">Annuler</button>                            </p>

                            </a></td>       
                                       
                                        
                                    </tr>


                                {% endfor %}
                                </tbody>
                            </table>
        </tbody>
    </table>

   
{% endblock %}
