<form method="post" action="{{ path('app_vol_delete', {'idVol': vol.idVol}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vol.idVol) }}">
   <button class="btn"><i class="fa fa-trash" style="color:#FF0000;font-size:20px"></i></button>
</form>
